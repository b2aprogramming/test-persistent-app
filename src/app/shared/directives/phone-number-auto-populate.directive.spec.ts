import { PhoneNumberAutoPopulateDirective } from './phone-number-auto-populate.directive';

describe('PhoneNumberAutoPopulateDirective', () => {
  it('should create an instance', () => {
    const directive = new PhoneNumberAutoPopulateDirective();
    expect(directive).toBeTruthy();
  });
});
